[project]
name = "niro"
version = "0.0.1"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Alex Severin", email = "severinalexeyv@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
niro = "niro.cli:main"

[build-system]
requires = ["uv_build>=0.12.2,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "coverage>=7.15.4",
    "pytest>=9.1.1",
    "ruff>=0.16.5",
    "ty>=0.0.75",
]
